// 题目：给定k个字符串数组，从这k个数组中任意取一个字符串，按顺序拼接，列出所有可能的字符串组合。

// 样例：
// input : [[“a”, “b”, “cd”], [“de”], [“e”, “f”]]
// output : [“adee”, “adef”, “bdee”, “bdef”, “cddee”, “cddef”]

/**
 * 
 * @param {Array} arr 
 * @return {Array} 
 */
function getResult(arr) {
  let result = []

}